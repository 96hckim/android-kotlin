<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ToDo" parent="Theme.Material3.DayNight">
    </style>

    <style name="Theme.ToDo" parent="Base.Theme.ToDo" />
</resources>